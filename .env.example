# ──────────────────────────────────────────────────────────
# NFL Script Generator — Environment Variables
# ──────────────────────────────────────────────────────────

# X (Twitter) API v2 — Bearer Token (Essential or higher)
# https://developer.twitter.com/en/portal/dashboard
X_BEARER_TOKEN=your_x_bearer_token_here

# OpenAI API Key
# https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Model selection
OPENAI_MODEL=gpt-4o

# Logging
LOG_LEVEL=INFO

# Output directory (relative to project root)
OUTPUT_DIR=output

# ──────────────────────────────────────────────────────────
# Tuning Parameters (optional overrides)
# ──────────────────────────────────────────────────────────
# Min engagement score to keep a tweet (0-100 scale)
MIN_ENGAGEMENT_SCORE=15

# Min credibility score to keep a tweet (0-100 scale)
MIN_CREDIBILITY_SCORE=25

# Max tweets to fetch per query
MAX_TWEETS_PER_QUERY=200

# Number of dominant narratives to extract
NUM_NARRATIVES=5

# Script target length in minutes
SCRIPT_TARGET_MINUTES=10
